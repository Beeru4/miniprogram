<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.xc.mapper.DayEventMapper">
    <select id="listDayEvent" resultType="DayEvent">
        SELECT `message`,`type`,`value`,`status`
         FROM `day_event`
    </select>
</mapper>